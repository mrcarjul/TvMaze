// precache images(improves performance on render)
const images = {
  episode_image: require('./no_episode_image.png'),
  logo: require('./logo.png'),
  show_image: require('./no_show_image.png'),
};

export default images;
